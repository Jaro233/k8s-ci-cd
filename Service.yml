kind: Service                             
apiVersion: v1
metadata:
  name: mrdevops
  labels:
    app: mrdevops
spec:
  type: LoadBalancer
  ports:  
    - port: 8080                               
      targetPort: 80                    
  selector:
    app: mrdevops                    
                   
